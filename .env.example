# Site
NEXT_PUBLIC_SITE_URL=https://nicholasjpanella.com

# LLM (OpenAI-compatible)
OPENAI_API_KEY=
OPENAI_BASE_URL=
LLM_MODEL=

# Optional
CHAT_MAX_TOKENS=1024

# Configure WAF rate limit in Vercel dashboard:
# Firewall → + New Rule → @vercel/firewall → rate limit ID: chat-messages (20 req/IP/hour)
